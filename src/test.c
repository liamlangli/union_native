#include <stdlib.h>

#include "global.h"

// f64 sample_sobol(u64 i, u32 dim)
// {
//   f64 r;
//   for (u32 k = 0; i; i >>= 1, ++k)
//     if (i & 1)
//       r ^= SobolMatrix[dim][k];
//   return r / (f64)(1 << M);
// }

int main()
{
  return 0;
}
